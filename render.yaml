services:
  - type: web
    name: aniwatch-api
    env: node
    branch: main
    buildCommand: npm install && npm run build
    startCommand: npm run start
    autoDeploy: true
    envVars:
      - key: ANIWATCH_API_DEPLOYMENT_ENV
        value: render
